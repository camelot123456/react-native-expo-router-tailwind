import { Text, View } from 'react-native';

const Account = () => {
  return (
    <View className="flex items-center justify-center w-full h-full bg-yellow-100">
      <Text>Account Screen</Text>
    </View>
  );
};

export default Account;
