import { Text, View } from 'react-native';

const Setting = () => {
  return (
    <View className="flex items-center justify-center w-full h-full bg-blue-100">
      <Text>Setting Screen</Text>
    </View>
  );
};

export default Setting;
