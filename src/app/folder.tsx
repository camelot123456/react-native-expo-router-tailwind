import { Text, View } from 'react-native';

const Folder = () => {
  return (
    <View className="flex items-center justify-center w-full h-full bg-red-100">
      <Text>Folder Screen</Text>
    </View>
  );
};

export default Folder;
