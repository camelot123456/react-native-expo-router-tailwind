import { Text, View } from 'react-native';

const Vocabulary = () => {
  return (
    <View className="flex items-center justify-center w-full h-full bg-green-100">
      <Text>Vocabulary Screen</Text>
    </View>
  );
};

export default Vocabulary;
